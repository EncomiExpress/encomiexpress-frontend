# ─── API Backend ─────────────────────────────────────────────────────────────
# URL base del backend. En desarrollo apunta al servidor local.
# En producción reemplazar con la URL del servidor desplegado (ej. Render).
VITE_API_URL=http://localhost:3000/api
